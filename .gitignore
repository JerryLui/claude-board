.DS_Store
node_modules/
*.log
.claude/worktrees/

# Working documents, kept local: the specs, tickets, design record, session
# vocabulary and internal audit reports. `git add -f <file>` is the whole reversal.
SPEC_*.md
TICKETS_*.md
DESIGN.md
CONTEXT.md
findings/

# design-sync: private tooling plus everything `extract-css.mjs` regenerates.
.design-sync/
.ds-sync/
ds-bundle/
